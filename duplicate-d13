#include<bits/stdc++.h>
using namespace std;
int remove(int arr[],int n)
{
    set<int>st;
    for(int i=0;i<n;i++)
    {
        st.insert(arr[i]);
    }
    for(auto it:st)
    {
        cout<<it<<" ";
    }
}
int main(){
int arr[8]={1,2,3,4,4,8,10,11};
remove(arr,8);
return 0;

}
