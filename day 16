//to check array is sorted is not
int isSorted(int n, vector<int> a) {
    for(int i=0;i<n;i++){
        if(a[i-1]<=a[i]){

        }else{
            return false;
        }
    }
    return true;
}
